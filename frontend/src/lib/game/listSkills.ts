const listSkills: string[] = [
    'h', 'j', 'k', 'l', //para players que jogam com a movimentação no wasd
    ' ', // neutro
    'z', 'x', 'c', 'v' //para players que jogam com a movimentação nas setas
];
// h e z => (humano), (monstro)
// j e x => (humano), quebrar parede (monstro)
// k e c => teleporte (humano),
// l e v => (humano), (monstro)

export default listSkills;
